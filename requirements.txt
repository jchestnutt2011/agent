ollama==0.6.2
streamlit==1.58.0
requests==2.34.2
ddgs==9.14.4
yfinance==1.5.1
pandas==3.0.3
faster-whisper==1.2.1
nvidia-cublas-cu12==12.9.2.10
nvidia-cudnn-cu12==9.24.0.43
beautifulsoup4==4.15.0
lxml==6.1.1
# Windows has no system zoneinfo database; tools/current_time.py's ZoneInfo
# lookups fail without this even for valid zone names. Pinned explicitly
# rather than relying on it arriving as a transitive dependency.
tzdata==2026.2
